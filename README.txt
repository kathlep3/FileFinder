Kathleen Pham
kathlep3@uci.edu

This assignment searches for an input that can either be "L" or "Q". "Q" will quit the program. "L" will list the directories, then prompt the user to input another letter, which will allow them to specify their search.